@using ThakyCompany.Language
@model List<ThakyCompany.Models.NewsDto>
<div class="panel panel-default">
    <div class="panel-heading boxtitle">
        <h3 class="panel-title">@Resource.News</h3>
    </div>
    <!-- List group -->
    <ul class="list-group">

        @foreach (var item in Model)
        {
            <li class="list-group-item">
                @Html.ActionLink(item.Title, "Detail", "News", new { id = item.ID }, null)
            </li>
        }
    </ul>
</div>