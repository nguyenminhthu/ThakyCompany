@using ThakyCompany.Language
@model List<ThakyCompany.Models.QuyTrinhDto>
<div class="panel panel-default">
    <div class="panel-heading boxtitle">
        <h3 class="panel-title">@Resource.Procedure</h3>
    </div>
    <!-- List group -->
    <ul class="list-group">

        @foreach (var item in Model)
        {
            <li class="list-group-item">
                @Html.ActionLink(item.Title, "Detail", "QuyTrinh", new { id = item.ID }, null)
            </li>
        }
    </ul>
</div>